pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/Yass93380/connected-combat-units.git'
            }
        }
        stage('Build') {
            steps {
                echo 'Building...'
                // Insert build commands here, e.g., mvn clean install
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
                // Insert test commands
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Insert deploy commands
            }
        }
    }
}
