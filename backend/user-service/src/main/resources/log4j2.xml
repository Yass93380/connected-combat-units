<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Properties>
        <Property name="LOGS_DIRECTORY">logs</Property>
        <Property name="MAX_FILE_SIZE">5MB</Property>
        <Property name="MAX_BACKUP_FILES">5</Property>
    </Properties>

    <Appenders>
        <!-- Fichier avec rotation -->
        <RollingFile name="FileLogger"
                     fileName="${LOGS_DIRECTORY}/app.log"
                     filePattern="${LOGS_DIRECTORY}/app-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n"/>
            <Policies>
                <!-- Rotation par taille et par date -->
                <SizeBasedTriggeringPolicy size="${MAX_FILE_SIZE}"/>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
            </Policies>
            <DefaultRolloverStrategy max="${MAX_BACKUP_FILES}"/>
        </RollingFile>

        <Console name="ConsoleLogger" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n"/>
        </Console>
    </Appenders>

    <Loggers>
        <!-- Logger APPLICATIF -->
        <Logger name="com.example.yass.users" level="debug" additivity="false">
            <AppenderRef ref="FileLogger"/>
            <AppenderRef ref="ConsoleLogger"/>
        </Logger>

        <!-- Spring Security -->
        <Logger name="org.springframework.security" level="debug" additivity="false">
            <AppenderRef ref="FileLogger"/>
            <AppenderRef ref="ConsoleLogger"/>
        </Logger>

        <!-- Hibernate -->
        <Logger name="org.hibernate" level="warn" additivity="false">
            <AppenderRef ref="FileLogger"/>
            <AppenderRef ref="ConsoleLogger"/>
        </Logger>

        <!-- Root logger -->
        <Root level="debug">
            <AppenderRef ref="FileLogger"/>
            <AppenderRef ref="ConsoleLogger"/>
        </Root>
    </Loggers>
</Configuration>
